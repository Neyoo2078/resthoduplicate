// export function countTo100In8Seconds() {
//   let currentPercentage = 1; // Start at 1%
//   const increment = 1; // Percentage increment

//   const interval = setInterval(function () {
//     // Update the current percentage
//     currentPercentage += increment;

//     // Display the current percentage
//     console.log(currentPercentage + '%');

//     // Check if we've reached 100%
//     if (currentPercentage >= 100) {
//       clearInterval(interval); // Stop the interval when we reach 100%
//     }
//   }, 80); // Update every 80 milliseconds (8 seconds / 100 increments)

//   //   return currentPercentage + '%';
// }

// // Call the function to start the countdown
// countTo100In8Seconds();
